{
  "routes": [
    {
      "method": "GET",
      "path": "/newsletter-subscribers",
      "handler": "newsletter-subscribers.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/newsletter-subscribers/count",
      "handler": "newsletter-subscribers.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/newsletter-subscribers/:id",
      "handler": "newsletter-subscribers.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/newsletter-subscribers",
      "handler": "newsletter-subscribers.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/newsletter-subscribers/:id",
      "handler": "newsletter-subscribers.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/newsletter-subscribers/:id",
      "handler": "newsletter-subscribers.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
